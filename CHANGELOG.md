## 3.19 — эффективные осадки и гидрологическая корректировка

- Дождевой триггер теперь использует эффективные осадки: морось почти не считается полезным увлажнением подстилки.
- Для сильных ливней введена небольшая потеря на поверхностный сток, при этом полноценный ливень сохраняет сильный грибной триггер.
- ET0 FAO оставлен единственным испарительным показателем: он уже интегрирует температуру, влажность, ветер и радиацию, поэтому эти факторы не дублируются коэффициентами.
- Базовый водный резервуар не перенастраивался: сохранены проверенные сценарии грибов и ягод.
- Устранён дублированный расчёт `acc` в `species_index()` без изменения результата.
- Добавлены три регрессионных теста эффективных осадков.

## 3.18 — видоспецифичная форма грибной волны

- Разные пороги короткого дождевого импульса по видам.
- Разная потребность в длительном увлажнении и чувствительность к засухе.
- Видоспецифичная форма лаг-ядра: ранний/поздний пик и ширина хвоста волны.
- Старый `rain_pulse()` сохранён как совместимый API; основная модель использует новые параметры.
- Добавлены регрессионные тесты параметров и нормировки ядер.

## 3.17 — процессная модель влаги и тепловой памяти

- Дождевой триггер теперь учитывает не только 3 суток, но и накопленные осадки за длинное окно.
- В сигнал введён фактический влагозапас верхнего слоя и штраф после продолжительной засухи.
- Температурный фактор использует многосуточную тепловую память и меньше реагирует на одиночный шумный день.
- Старый `rain_pulse()` сохранён для совместимости; новое ядро использует `effective_rain_pulse()`.
- Android и desktop используют идентичное расчётное ядро.

## 3.16 — грибной радар и фазы волны

Для пользователей:

- Добавлен «Грибной радар» на 7 дней по грибам текущего сезона: одна сводка показывает, что перспективно сегодня и в ближайшие дни.
- Для каждого вида определяется понятная фаза уже рассчитанной кривой: ожидание, зарождение, рост, пик, плато или спад.
- Радар показывает лучший день и силу индекса, но не выдаёт индекс за вероятность находки.
- Личный журнал не используется для обучения модели: прогноз остаётся погодной/биологической моделью.

Для разработчика:

- Логика радара вынесена в чистый `radar.py` без зависимости от Kivy; одинаковая копия добавлена для desktop.
- Добавлены тесты фаз, сортировки по лучшему дню и текстовой сводки.

## 3.15 — интерпретация прогноза без ложной точности

Для пользователей:

- В карточке грибного вида появился календарный прогноз: ближайшее непрерывное окно с индексом от 33/100 и ориентировочный день пика. Это представление уже рассчитанного индекса, а не отдельная модель.
- Добавлена «Устойчивость оценки» 0–100: она показывает, насколько согласованы погодные и сезонные факторы между собой. Подпись прямо предупреждает, что это не вероятность найти гриб.
- Дальнее окно ограничено 14 сутками, чтобы интерфейс не создавал впечатление точного прогноза далеко вперёд.
- Новые пояснения доступны в Android-карточке дня и в desktop-разделе «Почему».

Для разработчика:

- Новые чистые функции `forecast_window()`, `window_text()` и `confidence()` добавлены идентично в Android и desktop ядра.
- Добавлены тесты непрерывного окна, NaN/отсутствия окна и границ confidence.
- Полный набор: 1151 passed, 11 skipped.

## 3.14 — синхронизация ядра и техническая стабилизация

Для разработчика и пользователей:

- Android и desktop снова используют идентичную реализацию расчётного ядра: desktop получил уже применявшиеся в Android единый список суточных погодных переменных, общий разбор ответа Open-Meteo и переиспользуемое определение доступных почвенных слоёв. Алгоритм расчёта индекса не менялся.
- Версия расчётного модуля синхронизирована с версией приложения — 3.14.
- GPX-экспорт переведён с устаревшего `datetime.utcfromtimestamp()` на timezone-aware UTC; устранены 50 предупреждений тестового набора.
- Версия Android-пакета поднята до 3.14 (`numeric_version = 31400`).

# История версий

## 3.13 — кнопки больше не залипают

Разбор случая из леса: после работы в окне похода «Пауза» и «Нашёл!»
перестали нажиматься, хотя приложение было живо и счётчики шли.

Для пользователей:

- Окно с ошибкой теперь показывается по одному за раз, а повтор той же
  ошибки молчит минуту. Раньше сбой в такте, который идёт раз в секунду,
  за минуту открывал шесть десятков модальных окон — они и перехватывали
  все касания. Само окно могло и не успеть построиться: тогда на экране
  не было видно ничего, а кнопки уже не нажимались.
- Сбой в счётчиках больше не останавливает поход: такт записи переживает
  ошибку, пишет её в протокол и работает дальше.
- Карта сама снимает касания, по которым давно не было событий. Потерянное
  «отпустили» — из-за погасшего экрана, открытого окна или упавшего
  обработчика — раньше оставалось в учёте навсегда, и следующий одиночный
  тап складывался со старым, принимаясь за пинч-зум.
- В окне «Приём и сервис» добавилась строка о здоровье интерфейса: сколько
  ошибок поймано, сколько касаний висит в учёте карты, сколько было сбоев
  такта. По ней причина видна сразу, без фотографирования экрана.

Для модератора и для сборки (не пользовательская новость):

- `_Catcher` в `main.py`: флаг `_showing`, подавление повтора по заголовку
  на `SILENCE = 60 c`, снятие запрета по `on_dismiss`, счётчик `count`.
  Если `_sheet()` не вернул окно — запрет снимается сразу, иначе об
  ошибках больше не узнать. `_sheet()` теперь возвращает созданный Popup.
- `walkscreen._pump()` разделён на обёртку с перехватом и `_pump_once()`;
  в протокол пишутся только первый, десятый и сотый сбой — иначе за час
  набежало бы три с половиной тысячи строк.
- `mapview`: `_drop_stale()` перед каждым новым касанием, отметки времени
  `_touch_seen`, порог `STALE_S = 8 c`. В `on_touch_up()` uid убирается из
  учёта и в обычном проходе диспетчера, а не только в проходе для
  захваченных касаний.
- Двенадцать тестов в `test_ui_stuck.py`. Один из них сразу поймал, что
  вызов `_drop_stale()` не встал в `on_touch_down()`: между строками
  оказалось присваивание `_moved`, и замена не совпала.
- `test_markup.py` читает тело метода по отступу, а не срезом в 1200
  символов: метод вырос, проверяемая строка уехала за границу окна, и тест
  упал, хотя экранирование никуда не делось.

## 3.12 — запись в кармане

Исправление главной причины, по которой трек обрывался при погашенном
экране: процессор уходил в сон.

Для пользователей:

- Запись маршрута продолжается, когда телефон лежит в кармане. Раньше
  сервис оставался жив (значок в шторке висел), но система усыпляла
  процессор, и координаты переставали приходить — в треке оставалась
  ровная дыра, а метры не считались. Достаточно было разбудить экран,
  чтобы запись пошла дальше, отчего казалось, будто «кнопки не работают».
- Если система всё же убьёт процесс — из-за нехватки памяти или
  «оптимизации» производителя, — Android поднимет запись заново, и трек
  продолжится с того же места, а не начнётся с нуля.
- В окне «Сервис» появилась строка «Процессор: удержан / не удержан» —
  по ней сразу видно, в этом ли причина разрыва.

Для модератора и для сборки (не пользовательская новость):

- Новый модуль `wakelock.py`: PARTIAL_WAKE_LOCK через pyjnius. Разрешение
  WAKE_LOCK в манифесте стояло с самого начала, но самого вызова не было —
  значок фонового сервиса мешает системе убить процесс, но не мешает
  процессору уснуть. В глубоком сне питоновский цикл замирает между двумя
  `time.sleep()`, а обработчик координат, висящий на главном Looper'е,
  перестаёт вызываться.
- Лок берётся с ограничением в 14 часов и `setReferenceCounted(False)`:
  первое — чтобы упавший процесс не держал лок вечно, второе — чтобы один
  `release()` действительно отпускал, а не уменьшал счётчик.
- `service_tracker.py`: захват перед подпиской на провайдеров,
  `refresh()` в каждом обороте цикла (лок мог истечь или быть снят
  системой), освобождение во всех ветках выхода, включая аварийную.
  Разрыв в приёме теперь пишется в журнал вместе с состоянием лока —
  чтобы отличать «спутники под пологом» от «процессор уснул».
- `buildozer.spec`: `services = Tracker:service_tracker.py:foreground:sticky`.
  Файл трека дописывается, поэтому перезапуск сервиса не теряет пройденное.
  Сервис при старте проверяет флаг остановки: после штатного завершения
  система тоже может поднять его по sticky, и крутиться зря не нужно.
- Тринадцать тестов в `test_wakelock.py` на поддельном PowerManager:
  порядок вызовов, отсутствие счётного режима, ограничение по времени,
  перезахват истёкшего лока, безопасность повторного освобождения,
  устойчивость к отказу системы. Удержит ли процессор настоящий Android —
  проверке на сборочной машине не поддаётся, это остаётся телефону.

## 3.11 — 06.09.2026

Для пользователей:

- Раскраска карты по погоде («Раскрасить» в окне выбора места) стала
  заметно ярче. Раньше клетки красились той же бледной шкалой, что и
  цифра индекса на плашке, — на карте без цифры поверх это читалось тускло,
  местами почти не видно. Теперь у карты свой градиент: суше — жёлто-
  песочный, обильнее — насыщенный зелёный, и чем выше индекс, тем плотнее
  цвет, а не одинаково бледно на всех значениях.

Для модератора и для сборки (не пользовательская новость):

- Новая пара `HEAT_GRADIENT_DAY`/`HEAT_GRADIENT_NIGHT` в `palette.py`,
  переключается темой так же, как `LEVELS`/`SPECIES`/`RAIN`. Пять опорных
  точек 0/25/50/75/100, между ними — линейная интерполяция и цвета, и
  прозрачности разом (`heat_color()`), поэтому граница между соседними
  клетками разного индекса не режет глаз резким скачком.
- `mapview.py`: `_draw_heat()` берёт `palette.heat_color(cell.index)`
  вместо `palette.level_colors()` — старая шкала подбиралась под контраст
  цифры на плашке, где нижняя ступень нарочно почти белая (#F2F2EE), а на
  клетке карты никакой цифры нет, и та же почти-белая заливка поверх
  бежевых тайлов OSM попросту не видна.
- Неиспользуемая с этой правкой константа `HEAT_ALPHA` в `TileMap` убрана —
  прозрачность теперь часть самого градиента, растёт вместе со значением,
  а не фиксирована одним числом на весь диапазон.
- Восемь новых тестов в `test_palette.py`: концы градиента совпадают с
  определением, значения за пределами 0–100 не роняют отрисовку, соседние
  точки не дают резкого скачка, прозрачность растёт вместе со значением,
  день и ночь дают разные, но каждый по-своему различимые цвета.

## 3.10 — 06.09.2026

Исправление настоящей ошибки, найденной в поле: «снег сошёл» сентябрьской
датой в карточке ягоды. Причина глубже, чем показ одного числа, — см. ниже.

Для пользователей:

- Карточка ягоды (черника, брусника, малина, клюква) больше не пишет
  бессмысленную дату схода снега поздним летом или осенью. Там, где раньше
  стояла выдуманная дата, теперь честно «не удалось определить» — вместо
  уверенного, но неверного ответа.

Для модератора и для сборки (не пользовательская новость):

- Корень найден в `snowmelt_gdd()`. Запрос прогноза везёт всего 31 сутки
  истории назад (`PAST_DAYS`) — для весенних грибов, которых смотрят
  вскоре после настоящей весны, этого достаточно. Для ягоды, которую
  смотрят всё лето и осень, реальный сход снега почти всегда оказывается
  за пределами этого окна, и функция без единой проверки принимала за
  «сход» первое подходящее по форме событие в имеющихся данных — разовое
  похолодание почвы в конце лета, раннюю осеннюю порошу, растаявшую за
  день, а если снега в окне не было вовсе — само начало окна.
- Добавлена проверка `MELT_MONTHS` (февраль–июль): найденная дата обязана
  попасть в правдоподобный весенний диапазон, иначе результат — «не
  определено», а не уверенная, но выдуманная дата.
- Исправлен и более глубокий случай: когда снега нет нигде в доступном
  окне, раньше подставлялось его начало — и с каждой проверкой позже по
  сезону эта дата тихо ползла вперёд вместе с окном, унося за собой и
  накопленное тепло. Теперь в этом случае честно возвращается «не
  определено» вместо систематически смещённой даты.
- Следствие для ягоды, о котором стоит знать: с этой правкой ягода
  показывает содержательный индекс только в узком окне в 4–5 недель сразу
  после настоящего схода снега — то есть до того, как большинство ягод
  успевает зацвести. Вне этого окна (что и есть бо́льшая часть сезона
  ягодного сбора) индекс честно нулевой, а не выдуманный. Для полноценной
  работы всесезонно ягоде нужен отдельный, более долгоживущий источник
  даты схода снега, чем скользящее 31-суточное окно — эта работа не
  входит в исправление, она отдельная и обсуждается отдельно.
- Оба места фикса синхронизированы в `desktop/mushroom_forecast.py` и
  `android/mushroom_forecast.py`, идентично, как и весь остальной научный
  код проекта. Шесть новых тестов в `TestSpring`.

## 3.9 — 06.09.2026

Для пользователей:

- Полноэкранная реклама теперь показывается не только при запуске
  приложения, но и после законченных дел: после итогов похода, после
  закрытия журнала наблюдений, после закрытия карты выбора места. Не чаще
  одного раза в пять минут, из какой бы точки её ни попросили, и никогда —
  во время самого похода: экран записи маршрута по-прежнему не знает о
  рекламе вообще, ни о какой из двух подключённых сетей.

Для модератора и для сборки (не пользовательская новость):

- `interstitial.py`: флаг «один раз за сессию» заменён на перерыв по
  времени (`MIN_INTERVAL_S`, пять минут) — функция называется `maybe_show()`
  вместо `show_once()`, и вызывать её можно из любого подходящего места:
  решение «пора или ещё рано» модуль принимает сам, вызывающему коду
  ничего дополнительно проверять не нужно.
- Новые точки вызова в `main.py`: после сводки похода (`_walk_done`, с
  небольшой задержкой, чтобы объявление не спорило с показом сводки за
  экран), при закрытии журнала (`show_walk_journal`) и при закрытии карты
  выбора места (`pick_place`) — оба через `on_dismiss` открытого попапа, а
  не при открытии: приложение сначала показывает то, за чем человек
  пришёл, и только потом, возможно, рекламу.
- По дороге поймана и исправлена коллизия имён: локальная переменная
  `journal` в `show_walk_journal` совпадала с именем модуля `journal.py`,
  который импортируется в соседней функции того же файла — статический
  сторож (`test_module_api.py`) справедливо принял `journal.bind(...)` за
  обращение к CSV-модулю. Переменная переименована в `journal_popup`.

## 3.8 — 06.09.2026

Для пользователей:

- В прогноз и на график добавлены лесные ягоды: черника, брусника, малина,
  клюква. Каждая появляется в списке видов рядом с грибами и получила свой
  цвет на графике. Малина лучше всего показывает себя на вырубках и гарях,
  клюква — только на болоте; для обеих в списке типов леса появились новые
  пункты («Вырубка, гарь, опушка» и «Верховое болото»).

Для модератора и для сборки (не пользовательская новость):

- Ягода считается не так, как гриб. Гриб в этой модели — отклик на импульс:
  дождь проходит через окно задержки, и через несколько дней после
  конкретного увлажнения появляется волна плодовых тел. Ягода наливается
  весь сезон от одной даты цветения, и общий с грибами код для неё не
  годится — использована отдельная функция (`berry_index`), а фенология
  (дата цветения) вычисляется той же машиной накопленного тепла от схода
  снега, что уже работает для сморчка и строчка.
- Заморозок в цвету убивает весь сезон разом — новых цветков в этом году
  не будет; засуха в наливе делает ягоду мельче, но не убирает её; у клюквы
  отдельно учтено, что спелому плоду осенний заморозок не вредит, а по
  примете идёт ему на пользу — остальным ягодам, наоборот, вредит.
- Научное ядро (`Berry`, `BERRIES`, `berry_index`, `berry_explain`,
  `berry_plain_summary`, два новых биотопа) добавлено в обе копии
  `mushroom_forecast.py` — desktop и android — идентично; расхождение
  между ними по-прежнему только в сетевом слое загрузки погоды.
- При подключении к интерфейсу нашлась и исправлена настоящая ошибка:
  карточка «Почему такой индекс» падала бы с `StopIteration` в тот день,
  когда лучшим по индексу оказывалась ягода, а не гриб, — код искал
  объект вида только среди грибов. Исправлено для всех трёх мест, где
  выбранный вид разбирается на факторы (`explain`, `plain_summary` и их
  ягодные пары).

## 3.7 — 29.08.2026

Для пользователей:

- Новая кнопка «Раскрасить» в окне выбора места: красит видимый кусок карты
  по индексу — зелёное там, где было теплее и мокрее, светлое — где суше.
  Тип леса нарочно не спрашивается, считается смешанным везде, подпись под
  картой говорит об этом прямо: у модели индекс сильно зависит от биотопа,
  а приложение не знает, что где растёт, и спрашивать один биотоп на весь
  снимок значило бы дать ложную точность там, где ельник сменяется вырубкой
  через полкилометра. Карта отвечает на вопрос «где было теплее и мокрее»,
  а не «где растут грибы» — куда именно ехать, решает сам человек.

Для модератора и для сборки (не пользовательская новость):

- Сетка (`heatgrid.py`) подстраивается под то, что видно на экране: размер
  клетки растёт вместе с отдалением карты и не мельчит меньше шага
  разрешения самой погодной модели, число клеток по стороне никогда не
  превышает 6×6 — стоимость одного расчёта ограничена сверху всегда.
- Загрузка (`heatfetch.py`) сначала пробует один пакетный запрос на всю
  сетку разом; Open-Meteo, по имеющимся сведениям, принимает несколько
  точек одним вызовом, но это не проверялось на живом отклике — код не
  верит своей догадке слепо и при любом расхождении формы ответа откатывается
  на путь по одной точке, который в проекте уже год как работает. Первая
  проверка на реальном отклике Open-Meteo — на боевой сборке.

## 3.6 — 22.08.2026

Для пользователей:

- Ещё одна попытка починить «+», «−» и «Закрыть поход» после выключения
  экрана во время похода. Версия 3.4 чистила только общий список
  касаний Kivy — этого хватало не всегда: у самой карты похода
  оказался свой, отдельный учёт перетаскивания, который тем фиксом не
  трогало. Теперь чистится и он тоже.

## 3.5 — 22.08.2026

Для модератора и для сборки (не пользовательская новость):

- Найдена и исправлена причина, по которой реклама (и баннер Яндекса, и
  полноэкранная myTarget) не показывалась вовсе: gradle-зависимости
  самих SDK не были прописаны в сборке — Python-код был готов, а
  библиотек физически не было в APK. Добавлено
  `android.gradle_dependencies` в `buildozer.spec`.
- Версия Яндекс SDK зафиксирована на 7.16.0 нарочно — в 8.0.0 (апрель
  2026) `MobileAds` переименован в `YandexAds` с рядом других ломающих
  изменений, а код `ads.py` писан под старый API.

## 3.4 — 22.08.2026

Для пользователей:

- Починена реальная проблема из леса: после того как экран гас и снова
  включался во время записи похода, кнопки «+», «−» и «Закрыть поход»
  переставали отвечать. Это застревавшее в Kivy касание, не долетевшее
  до конца перед тем, как приложение сворачивалось, — теперь оно
  сбрасывается при возврате на экран.

## 3.3 — 22.08.2026

Для пользователей и модератора:

- Добавлена полноэкранная реклама myTarget при запуске приложения —
  вместе с уже существующим баннером Яндекс Рекламы. Обе отключаются
  одной покупкой «Без рекламы».
- Политика конфиденциальности и условия использования обновлены с учётом
  второй рекламной сети.

## 3.2 — 22.08.2026

Для пользователей:

- Плавное появление окна покупки, конфиденциальности и условий
  использования вместо мгновенного.
- Индикатор запроса: пока грузится прогноз погоды, надпись статуса
  показывает бегущие точки — понятнее, что приложение работает, а не
  зависло.
- Крупнее цифры дневного индекса плодоношения в списке дней.
- Увеличены слишком мелкие кнопки закрытия в паре окон.

## 3.1 — 22.08.2026

Для пользователей:

- Скруглённые уголки у окон и карточек вместо прямых.
- Кнопки заметно темнеют при нажатии — понятнее, что тап прошёл, особенно
  в тёмной теме.
- Короткая вибрация при успешной покупке «Без рекламы».

## 3.0 — 21.08.2026

Для модератора и пользователей:

- Добавлена политика конфиденциальности и условия использования —
  ссылки на оба документа доступны прямо на экране покупки «Без
  рекламы».
- Скриншоты в карточке приложения приведены к соотношению 9:16, чтобы
  магазин их больше не обрезал.
